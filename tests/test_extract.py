"""
TODO: Write unit tests for src/extract/extract_api.py.

Ideas:
- Test fetch_products() against a mocked API response (use pytest-mock's
  `mocker.patch` to fake `requests.get` — don't hit the real API in tests).
- Test save_raw() writes valid JSON to a temp directory (use the `tmp_path`
  pytest fixture).

Run with: pytest tests/ -v
"""
