"""
Optional: a shared logger so your extract/load scripts print consistent,
timestamped output instead of using bare print() statements.

TODO (optional, nice-to-have): write a get_logger(name) function using
Python's built-in `logging` module that returns a configured logger.
"""
import logging


def get_logger(name: str) -> logging.Logger:
    # TODO: implement this (or just use print() statements to start —
    # come back and clean this up later)
    raise NotImplementedError
